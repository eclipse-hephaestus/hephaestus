---
title: "header_wrapper_class"
description: "CSS class appended to the `header-wrapper` element on all pages. Used only when no page-level `header_wrapper_class` is set."
type: "doc_param"
category: "layout"
param_type: "string"
accepted_values:
  - Any valid CSS class name
hide_sidebar: true
---

## Example

```toml
[Params]
  header_wrapper_class = "header-default-bg-img"
```
