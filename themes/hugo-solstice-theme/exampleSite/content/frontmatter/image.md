---
title: "image"
description: "Fallback image URL for the page. Used as the social sharing thumbnail when `share_img` is not set."
type: "doc_param"
category: "metadata"
param_type: "url"
accepted_values:
  - Absolute path (e.g. `/images/hero.jpg`)
  - Fully qualified URL (e.g. `https://example.org/images/hero.jpg`)
hide_sidebar: true
---

## Example

```yaml
image: "/images/og-default.jpg"
```
